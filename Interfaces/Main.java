import java.io.File;
import java.io.FileNotFoundException;
import java.util.ArrayList;
import java.util.Collections;
import java.util.Scanner;

public class Main {
    // public static void main(String[] args) {
    /*
     * try { Scanner txt = new Scanner(new File("Interfaces/song.txt"));
     * ArrayList<String> file = new ArrayList<String>(); while (txt.hasNext()) {
     * file.add(txt.nextLine()); } for (String line : file) {
     * System.out.println(line); } System.out.println("\n" + "-------------------" +
     * "\n");
     * 
     * StringComparator secondChar = new StringComparator(); Collections.sort(file,
     * secondChar);
     * 
     * for (String line : file) { System.out.println(line); }
     * 
     * } catch (FileNotFoundException e) { e.printStackTrace(); }
     */
    // }
}
